#include<bits/stdc++.h>
using namespace std;
#define pb push_back
#define i64 long long int
#define sf scanf
#define pf printf
int main()
{
    int n,k,mx = 0;
    cin>>n>>k;
    int a;
    while(n--)
    {
        cin>>a;
        if((a-k)>mx) mx = a-k;
    }
    cout<<mx;
return 0;
}
