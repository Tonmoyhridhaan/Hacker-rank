#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,k;
    cin>>n>>k;
    int bill,bc,ba=0,i;
    for(i=0;i<n;i++)
    {
        cin>>bill;
        if(i!=k) ba+=bill;
    }
    ba/=2;
    cin>>bc;
    if(bc>ba) cout<<bc-ba;
    else if(bc==ba) cout<<"Bon Appetit";

return 0;
}
