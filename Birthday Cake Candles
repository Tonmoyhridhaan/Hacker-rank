#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,i,mx=0,cnt=0;
    cin>>n;
    int arr[n];
    for(i=0;i<n;i++)
    {
        cin>>arr[i];
        if(arr[i]>=mx) mx = arr[i];
    }
    for(i=0;i<n;i++)
    {
        if(arr[i] == mx) cnt++;
    }
    cout<<cnt;
    return 0;

}
